# Progress

## Current Status
- 解説内の数式だけの行を中央寄せにし、文中数式は本文の流れのまま表示されるように調整
- 統計分布表で、選択中の固定ヘッダーと左端見出しの背景を不透明にし、スクロール時に下のセル文字が重なって見える問題を修正
- `/room-sim` に、図面ベースの近似住戸を3Dで内覧する住宅購入検討用シミュレーターを追加
- 住戸幅6.2m、本体奥行き約12.2m、バルコニー奥行き約2.0mの初期データで、玄関・廊下・LDK・キッチン・洋室(1)・サービスルーム・洋室(2)・水回り・バルコニーの位置関係を再現
- 家具配置、家具クリック選択、床面ドラッグ移動、90度回転、Delete/Backspace削除、Esc選択解除、家具追加、表示切替、全体スタイル切替、選択家具の色・素材・表示方式切替を実装
- 俯瞰/内覧モード、定点カメラ、WASD/矢印キー移動、昼夜切替、目線高さ調整、スクリーンショット保存、localStorageによる家具・表示設定復元を実装
- `/loan-sim` に `単一試算 / シナリオ比較` の切替を追加し、比較モードでは共通条件とシナリオA/Bを分けて入力できるように調整
- 単一試算の保存条件をシナリオ比較のA/Bへ読み込めるようにし、比較ページのシナリオ欄は保存条件の選択中心に整理
- 単一試算にも固定/変動金利と変動金利の見通しを追加し、変動金利の一定間隔上昇では返済額・利息・残高を見直し月ごとに再計算するように調整
- 比較モードの初期値を、物件価格5,500万円・頭金500万円・投資の初期元本400万円、シナリオAは固定35年2.5%、シナリオBは変動35年1.5%で差額自動積立ONに調整
- 比較モードでは、単一試算の保存条件をシナリオの保存元として使い、比較専用のシナリオ保存UIは表示しない方針に調整
- 比較モードの初期資産入力を `投資の初期元本` として扱い、生活費や予備資金を含めず、頭金とも切り離して投資残高を開始するように調整
- 比較モードの `頭金` を共通条件から外してシナリオA/Bごとの入力へ移し、借入額と総支払利息の比較に反映
- 比較モードの借入額を `物件価格 + 諸費用 - 頭金` でシナリオごとに計算し、頭金差による総支払利息の違いをサマリーでも確認できるように調整
- 比較モードの `変動` 金利に、`一定金利で近似` と `一定間隔で上昇` の 2 モードを追加し、見直し月ごとに返済額・利息・残高・差額自動積立の元になる毎月返済相当額を再計算するように調整
- 比較モードで、純資産差（B - A）を主グラフにし、A/B の純資産・投資残高・ローン残高・累計運用益と利息を比較できるように調整
- 比較モードに `諸費用` と `投資の初期元本` を追加し、実際に投資へ回している残高を起点にシミュレーションするように調整
- 比較モードに `差額自動積立` と `完済後は返済相当額を積立` を追加し、35年返済 vs 40年返済+差額積立のような戦略比較を継続期間込みで見られるように調整
- 住宅ローン返済シミュレーターに `運用の元手` 入力を追加し、開始時点の運用残高を総資産グラフ・積立残高・運用益比較へ反映
- 住宅ローン返済シミュレーターの `総資産の推移` グラフに `ローン残高` ラインを重ね、さらに `累計運用益` と `累計ローン利息` を比較する新しいグラフを追加
- 住宅ローン返済シミュレーターの `総資産の推移` グラフを、構成要素ごとの色分け積み上げ表示に変更し、tooltip でも内訳を確認できるように調整
- 住宅ローン返済シミュレーターの `総資産の推移` グラフ右側に構成要素チェックボックスを追加し、物件価格と積立残高の反映対象を切り替えられるように調整
- 住宅ローン返済シミュレーターの `総資産の推移` グラフだけを 2 カラム幅で表示し、最初に見る全体グラフとして強調
- 住宅ローン返済シミュレーターの `計算ルール` カードを推移グラフの下へ移し、まずグラフを見てから前提を確認できる並びに調整
- 住宅ローン返済シミュレーターの既存グラフ群の最上段に `総資産の推移` を追加し、物件価格を一定とみなした資産総額の流れを最初に確認できるように調整
- 住宅ローン返済シミュレーター上段の `入力条件` / `サマリー` 見出し文字を外し、ボタンとカード内容だけ見える構成へ調整
- 住宅ローン返済シミュレーターの数値入力欄を、全消し後にそのまま打ち直せる挙動へ修正し、完済年月カードも表示から除外
- 住宅ローン返済シミュレーターの開始時点年齢と定年年齢を通常幅で横並びにし、借入条件欄の高さをさらに圧縮
- 住宅ローン返済シミュレーターの借入額欄を自動計算表示に固定し、直接入力トグルを外して操作欄を簡素化
- 住宅ローン返済シミュレーターのサマリーカードを、項目名と金額が同じ行に並ぶ構成へ変更し、数値比較をしやすく調整
- 住宅ローン返済シミュレーターの入力条件・サマリーから補助説明文をさらに整理し、定年時残高カードとボーナス月加算カードも外して上段を短く調整
- 住宅ローン返済シミュレーターの保存条件欄から不要な補助文言を外し、プリセット枠の余白も詰めて上段を省スペース化
- 住宅ローン返済シミュレーターの上段を、desktop で左に操作欄・右に1列サマリーの構成へ見直し、条件を触りながら金額の変化を横で追いやすく調整
- 住宅ローン返済シミュレーターに開始時点の年齢と定年年齢を追加し、定年時点の残り残高をサマリーで確認できるように調整
- 保存済み条件の localStorage 復元で、新しく追加した年齢項目が無い既存データも既定値補完で読み込めるように調整
- 統計分布表に F 分布表を追加し、右側 5% の臨界値を分子自由度・分母自由度の組み合わせで確認できるように調整
- F 分布のセル選択時も、既存の正規分布・t分布・カイ二乗分布と同様に右側棄却域を面積図で確認できるように調整
- 通常の問題回答画面と暗記画面のツールに「問題編集」を追加し、今開いている問題をその場で既存編集モーダル相当の UI で直せるように調整
- 問題管理画面の編集モーダルを共通コンポーネント化し、保存後は回答画面に留まったまま対象問題の表示内容を更新できるように整理
- 回答判定の前提が変わる編集では、その問題だけ `answers` / `answeredMap` / `showAnswerMap` / `confidences` / `memorizationInputsMap` / `memorizationLogs` / `questionElapsedMsById` など必要なセッション状態を安全側でリセットする helper を追加
- 問題管理画面に `AIで作成` 導線を追加し、依頼文から 1 問分の下書きを作る管理画面専用AIモーダルを実装
- AI生成結果は既存の問題編集フォームへ下書き投入し、既存の保存操作で登録する流れに整理
- 生成時に既存問題の近い候補を参照し、生成後にも簡易な重複警告を出す第一段階の重複回避を追加
- 問題回答画面の選択肢ショートカットを `1〜9` と `0` まで拡張し、5択以上でも表示とキー入力が揃うように調整
- 問題管理画面の編集モーダルで、問題文と解説を `入力 / プレビュー` タブで切り替え、Markdown のレンダリング結果を保存前に確認できるように調整
- ホーム画面の AppLauncher から開ける単独アプリとして、`/loan-sim` の住宅ローン返済シミュレーターを追加
- 住宅ローン返済シミュレーターで、元利均等 / 元金均等、毎月積立、ボーナス返済、固定費、グラフ、月別返済表を front-end only で扱える構成を実装
- 住宅ローン返済シミュレーターに年収入力を追加し、毎月返済額の年収比・概算手取り比をサマリーで確認できるように調整
- 住宅ローン返済シミュレーターで、条件を名前付きプリセットとしてローカル保存し、再読込・削除できるように調整
- 住宅ローン返済シミュレーターの推移グラフで、マウス位置に応じた年月・金額の tooltip を表示できるように調整
- 住宅ローン返済シミュレーターで、保存済み条件をプルダウン即時反映に切り替え、サマリ上段・入力条件下段の構成へ再整理
- ホーム画面の問題集カードに、作成順・問題数順・あいうえお順の並び替えと昇順 / 降順切り替えを追加
- タグフィルタの反対側にソート UI を配置し、一覧操作を同じ行でまとめて扱えるように調整
- 復習設定に、正解時の復習日を後ろへ分散する ON/OFF を追加
- 復習日分散は `nextDue` のみを対象にし、保存時に一度だけ offset を決めて schedule / log へ共有する形に整理
- 設定画面と復習ボードの説明文に、集中緩和目的・既存予定へ非遡及であることを追記
- Codex向けの運用ドキュメント追加方針を定義
- Boris風ワークフローを AGENTS.md ベースに読み替え
- 補助ドキュメントの初期雛形を追加
- ホームの AppLauncher から起動できる単独アプリ「ベクトル・行列ラボ」を追加
- ベクトル・行列ラボで、2x2 行列の線形変換・合成変換・行列式/逆行列・像/核を SVG 可視化で確認できる構成を実装
- ベクトル・行列ラボの非モバイル表示を、操作列と平面グラフを同時に見やすい 2 列寄りレイアウトへ調整
- ベクトル・行列ラボのグラフラベルを数式寄りに整え、タッチドラッグ時のスクロール競合を抑制
- ベクトル・行列ラボのパラメータ入力ラベルも KaTeX 表示へそろえ、記号の読み取りを統一
- ベクトル・行列ラボの iPad 向けに、ラベル描画を HTML オーバーレイへ切り替え、graph サイズと touch drag を再調整
- 解答画面の右上に、AIチャットと独立して増やせるツール導線を追加
- 初版ツールとして、問題画面を閉じずに使える電卓を実装
- 電卓をドラッグ移動・リサイズ可能にし、高度計算まで対応を拡張
- 電卓の未閉鎖括弧の補助表示と、関数単位のバックスペース削除を追加
- 電卓の不完全式 `=` を no-op にし、配置を科学電卓寄りに再整理
- 電卓表示の 3 桁区切りと、ボタン群が収まる最小ウィンドウサイズを追加調整
- 電卓に `±` / `%` / `AC/C` を追加し、比率固定の縮小表示にも対応
- 電卓の前置関数入力を、直前値を包む挙動へ修正
- 電卓のリサイズを縦横独立に戻しつつ、縮小時の中身スケールは維持
- 電卓の右下ハンドル限定リサイズ、全高を使うボタン配置、式履歴表示を追加
- 電卓の履歴表示欄を常設し、`=` 後の高さ変化を解消
- 学習画面で、document root が空白方向へパンしてしまうことがある挙動を抑えるスクロールロックを追加
- 学習画面の app shell を viewport 固定にして、ヘッダーが画面外へ逃げて戻れなくなる状態を抑制
- 手書きメモ欄のリサイズ中だけ root 全体をロックし、iPad での下方向ドラッグ競合を抑制
- 手書きメモ欄の自動スクロール先を学習画面内の scroll container に合わせ、remote 側の滑らか化・自動拡張と iPad 対策を統合
- 問題回答画面のツールに、表だけを素早く見られる統計分布表パネルを追加
- 問題画面の電卓に `1/x` を追加し、逆数入力をワンタップ化
- 問題画面の統計分布表パネルを右下ハンドルでリサイズ可能に調整
- 問題画面の統計分布表パネルを、電卓と同じくヘッダーのドラッグで移動可能に調整
- 統計分布表の t 分布で、自由度列の幅を少し狭めて値列を見やすく調整
- 電卓と統計分布表の閉じるボタンで、SVG アイコン押下時にもドラッグが優先されないよう判定を修正
- 問題画面の統計分布表パネルの初期サイズを、電卓と同じ基準サイズに統一

## Ongoing Focus
- 変更前に影響範囲を明確にする
- 既存共通部品の再利用を優先する
- verify と release note 更新の運用を定着させる

## Next Candidates
 - ベクトル・行列ラボのタブ別レイアウトや操作性の微調整
- `src/AGENTS.md` と `api/AGENTS.md` の導入
- verify 補助スクリプトの追加
- よく触る機能別の補助ドキュメント整備
